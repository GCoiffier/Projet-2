open Tests
open Formula
open Bdd.BDD
open API
open Minisat
open Tseitin

let _ = let t = Sys.argv in let n = t.size() in let entree = t(n-1) in
	match argv[1] with
	|"-minisat" -> 
	|"-tseitin" -> 
	|_ -> 
	
	
let do_BDD entree = 
